version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=http://localhost:5000/api
    restart: always

#  # Uncomment if you have the backend repo as a sibling
#  backend:
#    build: ../ResQMeals/backend
#    ports:
#      - "5000:5000"
#    environment:
#      - MONGO_URI=mongodb://mongo:27017/resqmeals
#    depends_on:
#      - mongo
#
#  mongo:
#    image: mongo:latest
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongo-data:/data/db

volumes:
  mongo-data:
